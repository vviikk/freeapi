import colors from './colors'; /* keeping this as a JSON as if there is a
                                     need to parse it into something legacy can use */
import theme from './theme';
import api from './api';
import constants from './constants';

export {
    colors,
    theme,
    api as API,
    constants as CONSTANTS,
};
